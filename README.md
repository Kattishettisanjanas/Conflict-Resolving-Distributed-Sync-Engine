# Conflict-Resolving-Distributed-Sync-Engine
Simulate multi-master sync (e.g., edge devices syncing to central DB) with automatic resolution of concurrent updates using vector clocks + CRDT-inspired merge logic in SQL.
